package model

case class TransformedUser(
                            accountId: Int,
                            fullName: String,
                            email: String
                          )
